{
  "name": "@kint/bruno_shared_scripts",
  "version": "1.0.0",
  "exports": "./es/index.js",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ./index.js --bundle --format=cjs --outdir=es"
  },
  "devDependencies": {
    "esbuild": "0.23.0"
  }
}
